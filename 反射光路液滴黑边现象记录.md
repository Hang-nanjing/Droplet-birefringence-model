# 反射光路液滴黑边现象记录

本文档记录反射光路实验中液滴边缘黑边现象的当前观察、初步解释和后续验证方向。这里的结论是工作假设，不是已经证明的最终原因。

## 实验素材索引

当前相关图片位于 `picture/` 目录：

| 文件 | 用途 |
|---|---|
| `picture/droplet1.png` | 反射光路下的液滴图像，用于观察左右两幅液滴图像及边缘黑边。 |
| `picture/droplet2.png` | 反射光路下的液滴图像，作为同类现象的重复样本。 |
| `picture/droplet3.png` | 反射光路下的液滴图像，作为同类现象的重复样本。 |
| `picture/校正.bmp` | 无液滴但带刻度尺的校正图，用于光路校正和尺度参考。 |
| `picture/无液滴.bmp` | 有针头但无液滴图像；图像中央竖向暗带来自镜架，与液滴本身无关。 |
| `picture/系统实物图.jpg` | 实验系统实物图，可用于理解 LED 板、相机、针头和反射装置的大致布置。 |
| `picture/无反射镜1.bmp` | 无反射装置时直接拍摄的液滴图像；相机和视场不同，主要用于判断液滴边缘黑边是否只由反射装置导致。 |
| `picture/无反射镜2.bmp` | 无反射装置时直接拍摄的液滴图像，作为直接拍摄的重复样本。 |

旧反射光路模拟源代码位于：

```text
D:\vs2022\FET\x64\源.cpp
```

其中记录了反射路径几何参数，可作为后续有限孔径追迹验证的参数来源。

## 观察到的现象

在反射光路拍摄的液滴图像中，可以看到两个液滴像：

- 左侧液滴图像的左侧有明显黑边。
- 右侧液滴图像的右侧有明显黑边。
- 两个黑边大致关于图像中心呈左右对称。
- 外侧黑边更大、更暗，内侧黑边相对较小。
- 图像中央竖向暗带来自镜架，是无液滴图像中也存在的结构暗带，与液滴折射本身无关。

一个容易困惑的地方是：根据反射光路追迹，两张液滴图像可以和液滴表面位置建立对应关系。左侧液滴的左侧与右侧液滴的内侧可能对应液滴的同一物理侧，左侧液滴的右侧与右侧液滴的外侧也可能对应另一物理侧。但实际黑边并没有按这种“物理表面左右对应”出现，而是更接近以图像中心为轴的外侧对称。

## 已知实验条件

- 背景不是散斑背景，而是一整块 LED 板。
- 反射光路中存在有限尺寸的反射镜、镜架和镜片厚度。
- 用户提供的镜架或反射结构尺寸信息为：长度约 `6 mm`，厚度约 `2 mm`。
- 旧模拟代码中的主要几何参数包括：
  - 物体或液滴区域中心约为 `(25, 0)`，物体尺寸 `3.0 mm`。
  - 反射镜厚度 `mirrorThickness = 2.1 mm`。
  - 下方反射镜：
    - `mirror1Down = (65, -15.6)`，长度 `8 mm`，角度 `50.5 deg`
    - `mirror2Down = (57, -3.8)`，长度 `6 mm`，角度 `62 deg`
  - 上方反射镜：
    - `mirror1Up = (65, 15.6)`，长度 `8 mm`，角度 `-50.5 deg`
    - `mirror2Up = (57, 3.8)`，长度 `6 mm`，角度 `-62 deg`
  - 相机光心 `(200.391, 0)`。
  - 焦距 `120.235 mm`。
  - 传感器长度 `8.445 mm`。

这些参数说明反射光路不是无限开口系统，而是一个强烈受有限镜面长度、镜架厚度和装调误差影响的系统。

## 当前判断：为什么会出现黑边

黑边很可能不是液滴表面上的固定黑色物理纹理，而是透明液滴边缘折射造成的强度重分布。

液滴边缘曲率大，光线穿过边缘区域时偏折角明显增大。对于 LED 板这种大面积发光背景，液滴边缘会把原本应进入相机的部分光线偏折走，也会把其他方向的光线重新汇聚或发散。这样在成像上可能形成亮边、暗边、焦散或反焦散结构。

直接无反射镜拍摄的液滴图像也可用于判断这一点：如果无反射装置时液滴边缘已经存在类似暗化趋势，那么液滴边缘强折射就是基础原因；反射装置主要是在这个基础上进一步改变暗边在哪一侧更明显、强度多大。

因此当前较稳妥的判断是：

- 基础机制：液滴边缘强折射导致进入相机的光通量减少或重新分布。
- 反射路径调制：有限镜面、镜架厚度、镜面边界、LED 板有效照明范围和镜头孔径共同决定哪些折射后的光线还能进入相机。

## 位置关系解释：为什么黑边近似居中对称

这里需要区分两个不同的“对应关系”：

1. **液滴物理表面对应关系**：某条追迹光路对应液滴真实表面的哪一侧。
2. **图像亮暗边对应关系**：某个像素附近的光线是否还能穿过完整有效光路，并最终接收到足够光强。

黑边的位置更直接受第二种关系控制。也就是说，黑边不一定严格跟随“液滴同一物理侧”的对应关系，而是跟随“被液滴强折射后的光线是否靠近有效孔径边界或遮挡区域”。

在反射系统中，左右两幅液滴像来自上下或左右不同的反射路径。由于两条路径在几何上近似镜像，图像外侧的边缘光线更可能同时接近各自路径的有效边界：

- 可能接近反射镜端点。
- 可能接近镜架或镜片厚度造成的遮挡区域。
- 可能离开 LED 板的有效均匀照明角度范围。
- 可能被相机镜头或光阑的边缘截断。

因此，即使“左侧液滴外侧”和“右侧液滴内侧”在液滴物理表面上存在对应关系，它们经过不同反射路径后，面对的有效孔径边界并不相同。最终图像上更容易看到的是左右两条反射路径各自外侧的共同失光，于是表现为大致关于图像中心对称的黑边。

外侧黑边大、内侧黑边小，可以理解为外侧边缘光线更接近系统有效通光边界，折射后更容易被截断或落到较暗的照明区域；内侧边缘虽然也发生强折射，但仍有更多光线能通过有效路径进入相机。

## 目前不能断言的部分

当前还不能确定黑边主要由哪一个因素贡献最大：

- 反射镜长度不够，边缘光线接近或超出镜面端点。
- 镜架或镜片厚度造成机械遮挡。
- LED 板发光角度或亮度分布不均匀。
- 相机镜头孔径、渐晕或视场边缘响应造成失光。
- 液滴本身折射形成的纯焦散/反焦散结构。
- 反射镜安装不完全对称导致左右黑边不完全对称。

这些因素可能同时存在。后续应通过几何追迹和对照实验拆分它们，而不是直接把黑边归因到单一原因。

## 后续验证计划

建议下一步先做有限孔径反射光路诊断，而不是直接进入完整三维 BOS：

1. 基于 `D:\vs2022\FET\x64\源.cpp` 中的反射镜几何，建立一份二维反射路径有限孔径检查程序。
2. 选取左右液滴图像的内侧边缘、外侧边缘和中心附近代表性光线。
3. 对每条光线记录：
   - 是否命中第一、第二反射镜的有效反射面。
   - 反射点到镜面端点的距离。
   - 是否穿过镜架厚度或机械遮挡区域。
   - 是否超出相机传感器或镜头有效孔径。
4. 分别比较左图外侧、左图内侧、右图内侧、右图外侧光线的通光裕量。
5. 如果外侧边缘光线系统性更接近镜面端点或遮挡区域，则“有限孔径/镜架截光”是假设的有力证据。
6. 将无反射镜直接拍摄图与反射光路图对比：
   - 若直接拍摄也有边缘暗化，说明液滴边缘强折射是基础机制。
   - 若反射光路外侧黑边显著增强，说明反射装置有限孔径或装调误差参与明显。
7. 将无液滴图作为背景校正参考：
   - 中央竖向暗带应从液滴黑边分析中排除。
   - LED 板非均匀亮度和镜架固定暗区应单独记录。

## 与当前代码的关系

当前 `main.cpp` 是二维共轴圆形液滴正向追迹程序，不包含反射镜、虚拟相机或真实 LED 板照明模型。

本文档记录的是下一阶段反射光路实验现象分析，不应直接改写当前二维共轴模型。更合适的做法是：

- 保留当前 `main.cpp` 作为基础折射追迹验证程序。
- 后续另建反射光路诊断模块，专门检查有限镜面、镜架和有效孔径对黑边的影响。
- 在反射路径诊断稳定后，再考虑和三维液滴模型、真实实验图像分析结合。
