# 液滴建模与光线追迹技术手册

本文档用于指导后续编写“透明液滴 BOS 光线追迹程序”。当前目标是先完成**上方虚拟相机 + 圆形/球形液滴 + 散斑背景板**的正向光线追迹，得到有无液滴时的背景板交点 \(Q_0\) 和 \(Q\)。

---

## 1. 当前技术路线

总体路线：

```text
1. 反射光路展开
   真实相机 + 反射镜系统
   → 两个虚拟相机

2. 先只取一个虚拟相机
   推荐先用上方虚拟相机

3. 建立液滴模型
   先二维圆形截面
   后三维球形
   再二维椭圆 / 三维椭球
   最后真实液滴轮廓曲面

4. 光线追迹
   有效 ROI 像素
   → 虚拟光心
   → 液滴
   → 散斑背景板

5. 求无液滴和有液滴的背景板交点
   Q0：无液滴
   Q ：有液滴

6. 得到理论 BOS 位移
   ΔQ = Q - Q0

7. 生成模拟散斑图
   I0(p) = T(Q0)
   I(p)  = T(Q)

8. 对模拟图像做 DIC
   验证 DIC 是否能恢复光线追迹真值

9. 后续与真实实验结合
   双视角轮廓初值 + BOS/DIC 位移优化
```

---

## 2. 坐标系统与参数

### 2.1 当前二维模拟坐标

当前 C++ 程序使用二维 \(x-y\) 平面。

| 轴 | 含义 |
|---|---|
| \(x\) | 主体光路大致方向 |
| \(y\) | 上下方向 |
| \(z\) | 当前二维程序中固定为 0 |

### 2.2 实物 / 液滴参数

| 参数 | 符号 | 数值 |
|---|---|---:|
| 液滴 / 物体中心 | \(O_d\) | \((25,0)\) mm |
| 液滴直径 | \(D_d\) | 3 mm |
| 液滴半径 | \(R\) | 1.5 mm |
| 液体折射率 | \(n_w\) | 1.33 |
| 空气折射率 | \(n_a\) | 1.0 |
| 散斑板到液滴后表面间距初值 | \(g\) | 0.5 mm |

### 2.3 真实相机等效参数

| 参数 | 符号 | 数值 |
|---|---|---:|
| 真实相机等效光心 | \(C_0\) | \((200.391,0)\) mm |
| 光心到传感器中点距离 | \(f\) | 120.235 mm |
| 传感器二维长度 | \(L_s\) | 8.445 mm |
| 传感器实际尺寸 |  | \(8.445 \times 7.065\) mm |
| 像元尺寸 |  | 3.45 μm |
| 放大倍率 |  | \(0.6\times\) |

真实传感器中点：

```text
S_mid = (cameraPos.x + focalLength, cameraPos.y)
      = (320.626, 0)
```

传感器端点：

```text
S_top    = (320.626,  4.2225)
S_bottom = (320.626, -4.2225)
```

### 2.4 反射镜参数

| 反射镜 | 中心 / mm | 长度 / mm | 角度 / ° |
|---|---:|---:|---:|
| M1 Down | \((65, -15.6)\) | 8 | 50.5 |
| M2 Down | \((57, -3.8)\) | 6 | 62 |
| M1 Up | \((65, 15.6)\) | 8 | -50.5 |
| M2 Up | \((57, 3.8)\) | 6 | -62 |

---

## 3. 反射光路展开为虚拟相机

### 3.1 镜面反射点公式

一条镜面直线由两端点 \(A\)、\(B\) 定义。点 \(P\) 关于该直线的镜像点 \(P'\) 可按如下步骤计算：

```text
v = B - A
t = dot(P - A, v) / dot(v, v)
H = A + t v
P' = 2H - P
```

其中 \(H\) 是 \(P\) 在镜面直线上的垂足。

### 3.2 物体虚像展开

真实光路：

```text
物体 → M1 → M2 → 相机
```

物体端展开：

```text
P_virtual = R_M2(R_M1(P_real))
```

这表示真实相机直接看虚像物体。

### 3.3 虚拟相机展开

后续重建和光线追迹应使用虚拟相机模型。相机端展开顺序与物体端相反：

```text
C_virtual = R_M1(R_M2(C_real))
```

同理，传感器中点和端点也按同样顺序展开：

```text
S_mid_virtual    = R_M1(R_M2(S_mid_real))
S_top_virtual    = R_M1(R_M2(S_top_real))
S_bottom_virtual = R_M1(R_M2(S_bottom_real))
```

### 3.4 当前虚拟相机参数

#### Down 路径

```text
C_down        = (211.5866, -73.7309)
S_mid_down    = (322.2635, -120.7104)
S_top_down    = (323.9133, -116.8236)
S_bottom_down = (320.6136, -124.5973)
```

物体中心主光线与传感器交点：

```text
P_img_down = (323.4430, -117.9317)
```

该点位于传感器上半区，因此 Down 路径对应上半 ROI。

#### Up 路径

```text
C_up        = (211.5866, 73.7309)
S_mid_up    = (322.2635, 120.7104)
S_top_up    = (320.6136, 124.5973)
S_bottom_up = (323.9133, 116.8236)
```

物体中心主光线与传感器交点：

```text
P_img_up = (323.4430, 117.9317)
```

该点位于传感器下半区，因此 Up 路径对应下半 ROI。

---

## 4. 光轴、主光线与 ROI

### 4.1 光轴

虚拟相机光轴由相机模型自身决定：

```text
虚拟光心 C → 完整传感器中点 S_mid
```

光轴不一定穿过液滴中心。

### 4.2 物体中心主光线

物体中心主光线为：

```text
液滴中心 O_d → 虚拟光心 C → 传感器上的成像点 P_img
```

这条线通常不经过传感器中点，而是落在对应 ROI 内。这说明液滴是离轴成像。

### 4.3 ROI 来源

ROI 不是独立的小相机，也不是新主点。每个虚拟相机仍有完整传感器平面，只是实际有效图像落在半幅区域。

当前对应关系：

| 路径 | 有效 ROI |
|---|---|
| Down 路径 | 上半 ROI |
| Up 路径 | 下半 ROI |

---

## 5. 第一阶段：二维圆形液滴光线追迹

### 5.1 目标

先只研究一个虚拟相机，建议从 **Up 路径虚拟相机** 开始。

目标是对其有效 ROI 上的每个采样点 \(P_s\)，计算：

```text
Q0：无液滴时打到散斑板的位置
Q ：有液滴时经两次折射后打到散斑板的位置
ΔQ = Q - Q0
```

### 5.2 从 ROI 点生成光线

设：

- \(P_s\)：传感器有效 ROI 上某一点；
- \(C\)：虚拟光心。

在针孔模型中，该像素对应的物方光线必须经过 \(C\)。

从光心向物方传播方向为：

```text
d0 = normalize(C - Ps)
```

注意方向：  
传感器在光心后方，物方在光心前方。因此从光心出发向物体和散斑板传播时应使用：

```text
C - Ps
```

而不是：

```text
Ps - C
```

光线方程：

```text
r(t) = C + t d0,  t > 0
```

---

## 6. 无液滴时求 \(Q_0\)

无液滴时，光线直接与散斑背景板相交。

如果散斑背景板在二维中为直线：

```text
x = x_bg
```

光线为：

```text
r(t) = C + t d0
```

则：

```text
t = (x_bg - C.x) / d0.x
Q0 = C + t d0
```

如果采用任意二维直线表示背景板，则可使用通用“射线—直线”求交。

---

## 7. 有液滴时求 \(Q\)

### 7.1 光线与圆形液滴求交

二维圆形液滴：

```text
|X - O_d|^2 = R^2
```

光线：

```text
X = C + t d0
```

代入得到二次方程：

```text
a t^2 + b t + c = 0
```

其中：

```text
oc = C - O_d
a = dot(d0, d0)
b = 2 dot(oc, d0)
c = dot(oc, oc) - R^2
disc = b^2 - 4ac
```

若：

```text
disc < 0
```

说明该光线未经过液滴，则：

```text
Q = Q0
```

若有两个正实根，取最小正根：

```text
P1 = C + t1 d0
```

这是第一次折射点。

### 7.2 第一次折射：空气进入水

前表面外法向：

```text
N1 = normalize(P1 - O_d)
```

介质：

```text
n1 = n_air
n2 = n_water
```

折射方向：

```text
d1 = refract(d0, N1, n_air, n_water)
```

### 7.3 液滴内部传播

从 \(P_1\) 出发：

```text
r1(t) = P1 + t d1
```

再次与圆求交，排除 \(t \approx 0\) 的原交点，取下一个正根：

```text
P2 = P1 + t2 d1
```

这是第二次折射点。

### 7.4 第二次折射：水进入空气

后表面外法向：

```text
N2 = normalize(P2 - O_d)
```

由于这次入射介质是液滴内部的水，传入折射函数的法向应指向入射侧，即：

```text
N_in = -N2
```

介质：

```text
n1 = n_water
n2 = n_air
```

折射方向：

```text
d2 = refract(d1, -N2, n_water, n_air)
```

### 7.5 出射光线与散斑板求交

出射光线：

```text
r2(t) = P2 + t d2
```

与散斑板求交，得到：

```text
Q
```

---

## 8. 向量形式 Snell 定律

### 8.1 为什么使用向量形式？

二维中当然可以用标量 Snell 定律：

```text
n1 sin(theta1) = n2 sin(theta2)
```

但用角度法需要维护很多符号规则：

- 法向是内法向还是外法向；
- 入射角正负；
- 折射角应加还是减；
- 水到空气时是否全反射；
- 后续三维中入射平面变化。

向量形式本质上仍然是 Snell 定律，但更适合程序实现，也能自然扩展到三维。

### 8.2 折射函数

输入：

| 符号 | 含义 |
|---|---|
| \(I\) | 入射方向，单位向量 |
| \(N\) | 法向，单位向量，指向入射介质侧 |
| \(n_1\) | 入射介质折射率 |
| \(n_2\) | 折射介质折射率 |

计算：

```text
eta = n1 / n2
ci = -dot(N, I)
k = 1 - eta^2 * (1 - ci^2)
```

若：

```text
k < 0
```

发生全反射。

否则：

```text
T = eta * I + (eta * ci - sqrt(k)) * N
T = normalize(T)
```

### 8.3 法向方向规则

传入 `refract()` 的法向必须始终指向入射介质一侧。

| 折射位置 | 介质变化 | 使用法向 |
|---|---|---|
| 前表面 | 空气 → 水 | 外法向 \(N_1\) |
| 后表面 | 水 → 空气 | \(-N_2\) |

---

## 9. 输出与检查

### 9.1 每条光线应输出

```text
Ps：传感器采样点
C：虚拟光心
d0：入射方向
Q0：无液滴背景板交点
是否命中液滴
P1：第一次折射点
d1：液滴内部方向
P2：第二次折射点
d2：出射方向
Q：有液滴背景板交点
ΔQ = Q - Q0
状态：no_hit / refracted / total_internal_reflection / invalid
```

### 9.2 必须检查的几何正确性

1. ROI 点、光心、液滴前折射点在入射前必须共线；
2. 进入液滴前光线不能弯曲；
3. 液滴内部光线为直线；
4. 出液滴后光线为直线；
5. 光线只在 \(P_1\)、\(P_2\) 两个界面处改变方向；
6. 未命中液滴的光线应直接传播到散斑板；
7. 物体中心主光线应落在正确 ROI 内；
8. 全反射应被标记，不应强行继续追迹。

---

## 10. 二维伪代码

```cpp
for each Ps in effective_ROI:

    // 1. Generate ray
    d0 = normalize(C - Ps)

    // 2. No droplet
    Q0 = intersectRayWithBackground(C, d0)

    // 3. With droplet
    hits = intersectRayCircle(C, d0, Od, R)

    if hits.empty():
        Q = Q0
        status = "no_hit"

    else:
        P1 = nearestPositiveHit(hits)
        N1 = normalize(P1 - Od)

        ok1, d1 = refract(d0, N1, n_air, n_water)
        if !ok1:
            status = "TIR_at_entry"
            continue

        hits2 = intersectRayCircle(P1, d1, Od, R, eps)
        P2 = nextPositiveHit(hits2)
        N2 = normalize(P2 - Od)

        ok2, d2 = refract(d1, -N2, n_water, n_air)
        if !ok2:
            status = "TIR_at_exit"
            continue

        Q = intersectRayWithBackground(P2, d2)
        status = "refracted"

    deltaQ = Q - Q0
```

---

## 11. 从二维扩展到三维

二维跑通后，下一步是三维球形液滴。

### 11.1 三维传感器平面

三维传感器需要：

```text
S_mid：传感器中心
e_u：传感器横向单位向量
e_v：传感器纵向单位向量
```

任意像素点：

```text
Ps = S_mid + u e_u + v e_v
```

### 11.2 三维球

```text
|X - O_d|^2 = R^2
```

射线：

```text
X = C + t d
```

求交公式与二维类似。

### 11.3 三维散斑板

散斑板为平面：

```text
dot(X - B0, N_bg) = 0
```

射线与平面求交：

```text
t = dot(B0 - O, N_bg) / dot(d, N_bg)
Q = O + t d
```

### 11.4 三维折射

折射函数完全相同，只是向量从二维变为三维。

---

## 12. 模拟图像生成

在已知 \(Q_0\) 与 \(Q\) 后，可以生成模拟 BOS 图像。

设散斑板纹理函数为：

```text
T(X, Y)
```

则：

```text
I0(p) = T(Q0(p))
I(p)  = T(Q(p))
```

其中：

| 图像 | 含义 |
|---|---|
| \(I_0\) | 无液滴模拟参考图 |
| \(I\) | 有液滴模拟畸变图 |

然后可对 \(I_0\)、\(I\) 做 DIC：

```text
DIC(I0, I) → u_dic
```

并与光线追迹理论位移比较。

---

## 13. 模拟 DIC 的意义

模拟 DIC 不是实验，而是虚拟数值实验。

它可以回答：

- 光线追迹生成的散斑畸变是否可被 DIC 匹配；
- 当前散斑板距离 \(g=0.5\) mm 是否合适；
- 散斑点大小是否合适；
- DIC 窗口大小是否合适；
- 哪些区域会失配或焦散。

真实实验前，建议通过模拟 DIC 筛选参数。

---

## 14. 焦散和可逆性判断

后续可计算局部映射雅可比：

```text
J = ∂Q / ∂p
```

若：

```text
det(J) ≈ 0
```

说明局部强压缩，可能焦散。

若：

```text
det(J) < 0
```

说明映射折叠，DIC 匹配会不可靠。

这些区域应在后续 BOS 反演中剔除或降低权重。

---

## 15. 推荐开发顺序

### 第 1 步：二维单相机追迹

```text
Up 虚拟相机
二维圆形液滴
一维 ROI 采样
散斑板直线
输出 Q0、Q、ΔQ
```

### 第 2 步：二维可视化

绘制：

```text
ROI 点 → 光心 → P1 → P2 → 散斑板
```

要求所有空气段和液滴内部段都是直线，只在界面处折线。

### 第 3 步：二维位移曲线

输出：

```text
横轴：ROI 采样位置
纵轴：ΔQ
```

### 第 4 步：三维球形追迹

```text
Up 虚拟相机完整 ROI
三维球形液滴
散斑板平面
输出 Q0(u,v)、Q(u,v)
```

### 第 5 步：生成模拟散斑图

```text
I0(p) = T(Q0)
I(p)  = T(Q)
```

### 第 6 步：模拟 DIC

比较：

```text
u_true from ray tracing
u_dic from DIC
```

### 第 7 步：加入 Down 虚拟相机

完成双虚拟相机 BOS 正向模型。

### 第 8 步：椭圆 / 椭球液滴

将圆/球模型升级为椭圆/椭球。

### 第 9 步：真实轮廓曲面

引入双视角轮廓重建得到的液滴初始形貌。

---

## 16. 当前最小可行任务说明

可以交给 Codex 的首个任务：

```text
请实现一个二维 BOS 光线追迹程序。
输入为：
- Up 路径虚拟相机光心 C_up；
- Up 路径虚拟传感器端点 S_top、S_bottom；
- Up 路径有效 ROI 为下半区；
- 液滴圆心 Od = (25, 0) mm；
- 液滴半径 R = 1.5 mm；
- 水折射率 n_water = 1.33；
- 空气折射率 n_air = 1.0；
- 散斑板为一条二维直线，先根据 g=0.5 mm 人为设置。

程序对 ROI 上若干采样点 Ps：
1. 计算 d0 = normalize(C - Ps)；
2. 无液滴时求 Q0；
3. 有液滴时求 P1、d1、P2、d2、Q；
4. 输出 Q0、Q、ΔQ；
5. 绘制若干光线路径；
6. 标记 no_hit、refracted、TIR、invalid 等状态。
```

---

## 17. 最重要的程序原则

1. 所有光线都从有效 ROI 像素出发，经虚拟光心进入物方空间；
2. 无效 ROI 不参与追迹；
3. 进入液滴前，光线必须是直线；
4. 只有在液滴界面处才改变方向；
5. 折射计算使用向量形式 Snell 定律；
6. 法向方向必须指向入射介质侧；
7. 先求 \(Q_0\)、\(Q\)，再考虑生成图像和 DIC；
8. 不要一开始直接做形貌反演。
