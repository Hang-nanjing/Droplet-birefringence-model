# Agent Project Context: Transparent Droplet BOS Ray Tracing

本文档是本项目的主要上下文入口，替代原 `README.md`。后续 AI agent 或协作者应优先阅读本文档，再阅读两份研究背景文档和 `main.cpp`。

## 项目目标

本项目研究透明悬挂液滴的 BOS（Background Oriented Schlieren）正向光线追迹。研究对象是针头出口端生成的透明液滴，液滴表面没有稳定纹理，相机主要看到背景散斑图案被液滴折射后的结果。

当前阶段不是做真实实验反演，也不是做完整三维重建，而是先建立一个可验证的正向模拟程序：

```text
已知液滴几何形状
    -> 计算无液滴时背景板交点 Q0
    -> 计算有液滴时背景板交点 Q
    -> 得到液滴造成的理论位移 DeltaQ = Q - Q0
```

长期路线是：

1. 双视角轮廓重建得到液滴初始外形。
2. BOS/DIC 提取背景散斑位移。
3. 光线追迹预测折射位移。
4. 将轮廓误差、BOS 位移误差、光滑性和体积约束放入优化框架。

## 当前阶段边界

当前代码只做二维共轴模型：

- 不考虑反射镜。
- 不考虑上下方虚拟相机。
- 不考虑双视角。
- 不做三维球形追迹。
- 不生成模拟散斑图。
- 不做 DIC。

当前模型假设：

- 光轴为 `x` 轴。
- 传感器上下方向为 `y` 轴。
- 所有点都在二维平面内，`z = 0`。
- 背景板、液滴圆心、相机光心共轴。

## 当前实现状态

入口文件是 `main.cpp`，构建目标是 `bos_ray_trace`。

程序已经实现：

- 从传感器中心开始，按 `100` 像素步长向上、向下生成采样点。
- 无液滴时，每条光线从采样点经相机光心反向追迹到背景板，得到 `Q0`。
- 有液滴时，判断光线是否命中二维圆形液滴。
- 未命中液滴时，令 `Q = Q0`，状态为 `no_hit`。
- 命中液滴时，计算：
  - 第一次折射点 `P1`
  - 液滴内部方向
  - 第二次折射点 `P2`
  - 出射方向
  - 有液滴背景板交点 `Q`
- 使用向量形式 Snell 定律处理空气到水、水到空气两次折射。
- 输出状态统计、位移范围、折射点坐标和折射角。
- 生成全局 SVG、局部 SVG 和 CSV 位移表。

## 关键参数

当前参数集中在 `main.cpp` 开头的结构体中。

### `SystemParameters`

| 参数 | 当前值 |
|---|---:|
| 背景板位置 | `x = 0` mm |
| 背景板高度 | `15.0` mm |
| 相机光心 | `(200.391, 0)` mm |
| 焦距 | `120.235` mm |
| 传感器中心 | `(320.626, 0)` mm |
| 传感器高度 | `8.445` mm |
| 像元尺寸 | `0.00345` mm |
| 采样步长 | `100` 像素，即 `0.345` mm |
| 液滴圆心 | `(2, 0)` mm |
| 液滴半径 | `1.5` mm |
| 空气折射率 | `1.0` |
| 水折射率 | `1.33` |

### `OutputFiles`

| 文件 | 含义 |
|---|---|
| `no_droplet_trace.svg` | 无液滴光线路径 |
| `droplet_trace.svg` | 有液滴光线路径 |
| `ray_trace_comparison.svg` | 无液滴虚线、有液滴实线对比图 |
| `near_field_trace.svg` | 背景板和液滴附近局部放大图 |
| `displacement_table.csv` | 每个采样点的位移表 |

### `VisualizationSettings`

控制 SVG 高度和局部放大图范围。当前局部图重点显示背景板和液滴附近区域，用于检查 `P1/P2`、法线、入射角和折射角。

## 输出说明

运行程序后，终端会输出：

- 系统参数。
- 每个采样点的 `Q0`、`Q`、`DeltaQ` 和状态。
- 状态统计：
  - `no_hit`
  - `refracted`
  - `TIR`
  - `invalid`
- `DeltaQ.y` 范围。
- `|DeltaQ|` 范围。
- `Refraction detail`：
  - 折射光线编号
  - 传感器采样点 `sensor_y`
  - `P1(x,y)`
  - `P1` 入射角/折射角
  - `P2(x,y)`
  - `P2` 入射角/折射角

当前典型结果：

```text
Total samples: 25
Status counts: no_hit=20, refracted=5, TIR=0, invalid=0
DeltaQ.y range: [-0.951246, 0.951246] mm
|DeltaQ| range: [0.000000, 0.951246] mm
```

## SVG 可视化约定

- 全局 SVG 使用真实物理比例绘图，`x` 和 `y` 方向使用统一毫米到像素比例。
- 背景板高度为 `15 mm`。
- 液滴直径为 `3 mm`。
- 因此图中液滴直径应为背景板高度的 `1/5`。
- `ray_trace_comparison.svg` 中：
  - 虚线表示无液滴光线路径。
  - 实线表示有液滴光线路径。
  - 同一采样点使用同一种颜色。
- `near_field_trace.svg` 中：
  - 只显示液滴和背景板附近区域。
  - 折射光线在 `P1/P2` 处绘制细实线法线。
  - 法线颜色与对应光线一致。
  - 角度用圆弧连接光线和法线，并在圆弧外侧标注数值，例如 `49.38 deg`。
  - 不再标注 `normal` 文字，避免遮挡。

## 构建与运行

推荐使用 CMake：

```powershell
cmake -S . -B build
cmake --build build
.\build\Debug\bos_ray_trace.exe
```

如果使用 MinGW，也可以：

```powershell
g++ -std=c++17 -Wall -Wextra main.cpp -o build\mingw_bos_ray_trace.exe
.\build\mingw_bos_ray_trace.exe
```

`CMakeLists.txt` 中已对 MSVC 添加 `/utf-8`，用于避免中文注释或字符串在 Windows 本地代码页下触发编码问题。

## 当前代码结构

`main.cpp` 主要结构如下：

- `Vec2` 和向量工具函数：二维向量、归一化、角度计算。
- `SystemParameters`：物理参数和采样参数。
- `OutputFiles`：输出文件名。
- `VisualizationSettings`：SVG 视图参数。
- `TraceResult`：单条光线追迹结果，包含 `Q0/Q/DeltaQ/P1/P2/法线/角度`。
- `TraceSummary`：状态统计和位移范围。
- `traceSample()`：核心二维光线追迹。
- `SvgWriter`：SVG 绘图工具。
- `writeTraceSvg()`：无液滴或有液滴全局图。
- `writeComparisonSvg()`：全局对比图。
- `writeNearFieldSvg()`：局部放大图和角度标注。
- `writeCsv()`：位移表格输出。
- `printResults()`、`printSummary()`、`printRefractionDetails()`：控制台输出。

## 验证重点

修改代码后至少检查：

1. 程序能通过 CMake 构建和运行。
2. 五个输出文件都能生成。
3. `no_hit` 光线满足 `Q = Q0`。
4. 中心光线穿过液滴中心，入射角和折射角接近 `0 deg`。
5. 上下对称采样点的位移大小相等、符号相反。
6. `near_field_trace.svg` 中法线、圆弧和角度标注不明显遮挡主光线。
7. 液滴直径与背景板高度的可视比例保持 `3:15 = 1:5`。

## 后续开发计划

当前二维共轴圆形液滴模型已经比较完善。建议下一步按以下顺序推进：

1. 继续小范围清理二维程序，把追迹、输出、SVG 可视化拆成更清楚的函数或文件。
2. 引入更合理的背景板位置参数，例如液滴后表面与背景板间距 `g`。
3. 生成二维位移曲线图，单独观察 `DeltaQ.y` 随采样位置变化。
4. 扩展到三维球形液滴：
   - `Vec2 -> Vec3`
   - 圆求交 -> 球求交
   - 背景线 `x = const` -> 背景平面
   - 一维传感器采样 -> 二维传感器网格采样
5. 三维跑通后，再考虑模拟散斑图生成。
6. 后续再进入模拟 DIC、反射光路、虚拟相机和真实实验数据。

## 不要过早做的事

当前阶段不要直接跳到：

- 完整 BOS 反演。
- 真实图像 DIC。
- 反射镜展开和上下虚拟相机。
- 双视角轮廓融合。
- 椭球或真实自由曲面优化。

这些都属于后续阶段。当前优先保持二维正向追迹可信、可视化清楚、数值输出便于验证。

## 参考文档

- `液滴三维形貌重建技术研究.md`：研究背景、文献启发、总体路线、反射镜系统和虚拟相机概念。
- `液滴建模与光线追迹技术手册.md`：二维/三维光线追迹公式、Snell 定律实现规则、推荐开发顺序。
- `反射光路液滴黑边现象记录.md`：反射光路实验中液滴边缘黑边现象、当前解释、未知点和后续验证计划。
- `main.cpp`：当前二维共轴 BOS 正向追迹程序。
- `CMakeLists.txt`：CMake 构建配置。
- `.vscode/tasks.json`：VS Code 构建和运行任务。
