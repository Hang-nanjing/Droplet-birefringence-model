# 液滴三维形貌 BOS 重建：项目总结

本文档用于在新项目中快速恢复当前研究上下文。研究对象为**针头出口端瞬态变化的透明悬挂液滴**，目标是建立一套从**双视角轮廓重建**逐步升级到 **BOS/DIC 折射位移修正**的透明液滴三维形貌测量方案。

---

## 1. 研究对象与实验问题

### 1.1 被测对象

被测对象是从针头出口端生成的透明液滴。其特点是：

- 液滴透明，表面本身无纹理；
- 液滴随时间变化，属于瞬态自由界面；
- 液滴形态可能接近球形、拉长椭球形，后期可能出现颈缩与脱落；
- 直径当前按约 **3 mm** 考虑；
- 液体为水，折射率约 **1.33**；
- 液滴在光学上近似一个强曲率透明透镜。

### 1.2 为什么不能直接用普通双目匹配？

普通双目立体视觉依赖被测表面的纹理或特征点。透明液滴表面没有稳定纹理，相机看到的主要是背景图案被液滴折射后的结果，而不是液滴表面本身。因此，普通双目匹配难以直接用于透明液滴表面点的三维重建。

### 1.3 为什么不能一开始直接做完整 BOS 反演？

BOS/DIC 方法匹配的是背景散斑图案，不是液滴表面纹理。针头悬挂液滴近似一个小球形透镜，光线会经历前后两个界面折射：

```text
散斑背景板
    ↓
空气
    ↓
液滴后表面：第一次折射
    ↓
液滴内部
    ↓
液滴前表面：第二次折射
    ↓
虚拟相机 / 相机
```

由于液滴边缘曲率大，容易出现：

- 背景图案强烈拉伸、压缩；
- 焦散；
- 多光路；
- DIC 匹配失败；
- 位移场不可逆或局部折叠。

因此更稳妥的技术路线是：

```text
双视角轮廓重建作为基础
        ↓
得到每一时刻液滴初始三维外形
        ↓
BOS/DIC 折射位移作为后续修正
        ↓
通过光线追迹与优化提高形貌精度
```

---

## 2. 已梳理文献与方法启发

### 2.1 “一种基于数字图像相关和折射效应的动态液滴形貌测量方法”

该文献对本课题非常重要。其核心是：

```text
基底散斑图案
    ↓
透明液滴折射
    ↓
DIC 得到散斑位移
    ↓
折射几何模型恢复液滴形貌
```

但它主要适用于**基底上的液滴**，通常可以将液滴表面表示为单值高度函数：

```text
z = h(x, y)
```

而针头悬挂液滴是闭合透明曲面，光线经历前后两次折射，不能直接套用该文献的单界面递推模型。

对本课题的启发是：

> 可以利用散斑图案位移反映透明液滴的折射作用，但需要结合液滴几何初值和光线追迹模型，而不是直接照搬基底液滴模型。

### 2.2 “基于立体视觉和全局优化的动态流体表面三维重建”

该类方法强调：

- 背景折射位移；
- Snell 定律；
- 跨视角一致性；
- 位置—法向一致性；
- 全局优化。

对本课题的启发是：

> 后续应将 BOS 位移、轮廓重投影误差、表面光滑性和体积约束统一到一个优化框架中。

### 2.3 轴对称 / 双视角轮廓液滴重建文献

这类文献说明，液滴轮廓可用于恢复液滴三维形貌，尤其在液滴近似轴对称或弱非轴对称时，可用：

- 单视角旋转体模型；
- 双视角椭圆截面模型；
- 视觉外壳模型。

对本课题的启发是：

> 先利用双视角轮廓重建液滴大形，是当前最稳的初始方案。

### 2.4 透明固体折射重建文献

包括透明球体、玻璃半球、透明装饰物重建等。这些文献考虑了透明物体的两次折射，但多针对静态固体，通常需要多位置显示器、复杂编码或长时间采集。

对本课题的启发是：

> 两界面折射建模很重要，但实验采集方式不能直接用于瞬态液滴。

---

## 3. 最终确定的总体路线

当前推荐技术路线为：

```text
阶段 1：双视角轮廓重建
阶段 2：圆形 / 球形液滴 BOS 正向光线追迹
阶段 3：生成模拟 BOS 图像并做模拟 DIC 验证
阶段 4：椭圆 / 椭球液滴模型
阶段 5：双视角轮廓初值 + BOS/DIC 位移优化
阶段 6：真实瞬态液滴三维形貌重建
```

### 3.1 阶段 1：双视角轮廓重建

使用单相机 + 反射镜系统，在同一帧中得到液滴两个方向的投影。可选模型：

| 模型 | 思路 | 适用性 |
|---|---|---|
| 单视角旋转体 | 一个侧视轮廓绕轴旋转 | 液滴近似轴对称时 |
| 双视角椭圆截面 | 每个高度用两个视角宽度构造椭圆 | 当前最推荐 |
| 双视角视觉外壳 | 两个轮廓共同裁剪空间体素 | 更通用，但两个视角仍有限 |

### 3.2 阶段 2：BOS 正向光线追迹

先不反演液滴形状，而是解决正向问题：

```text
已知液滴形状
    ↓
预测背景散斑图案如何被折射
```

即对每个 ROI 像素求：

```text
Q0：无液滴时光线打到散斑板的位置
Q ：有液滴时光线经两次折射后打到散斑板的位置
```

二者差异为：

```text
ΔQ = Q - Q0
```

### 3.3 阶段 3：图像生成与模拟 DIC

在得到 \(Q_0\) 和 \(Q\) 后，可生成：

```text
I0：无液滴模拟散斑图
I ：有液滴模拟散斑图
```

然后对二者做 DIC，得到模拟 DIC 位移场，并与光线追迹的理论真值比较。这一步不是物理实验，而是数值实验，用于验证 DIC 能否可靠提取液滴造成的散斑位移。

### 3.4 阶段 4：从球形到椭球形

老师建议的路线是合理的：

```text
球形液滴透镜
    ↓
拉长为椭球形透镜
    ↓
再用真实轮廓重建形貌
```

先用球形/圆形模型跑通折射流程，再扩展到椭圆、椭球和真实液滴曲面。

### 3.5 阶段 5：轮廓与 BOS 融合优化

后续目标函数可写成：

```text
E(S) =
w1 * E_silhouette
+ w2 * E_BOS
+ w3 * E_smooth
+ w4 * E_volume
```

其中：

| 项 | 含义 |
|---|---|
| \(E_{\text{silhouette}}\) | 三维模型投影轮廓与双视角轮廓的误差 |
| \(E_{\text{BOS}}\) | 光线追迹预测位移与 DIC 实测位移的误差 |
| \(E_{\text{smooth}}\) | 表面光滑约束 |
| \(E_{\text{volume}}\) | 体积或注射量约束 |

---

## 4. 反射镜系统与两个虚拟相机

### 4.1 真实系统

真实系统中只有一个相机，但通过两条反射光路观察同一个液滴：

```text
真实液滴 → 上方反射镜路径 → 真实相机
真实液滴 → 下方反射镜路径 → 真实相机
```

传感器上形成两个区域：

```text
上半区域：对应下方反射路径
下半区域：对应上方反射路径
```

### 4.2 虚拟相机等效

对于重建与光线追迹，应采用：

```text
一个真实液滴 + 两个虚拟相机
```

而不是：

```text
一个真实相机 + 两个虚像液滴
```

两种表示等价，但虚拟相机表示更适合重建。

如果真实光路为：

```text
真实液滴 → M1 → M2 → 真实相机
```

则虚拟相机从相机端展开：

```text
真实相机 → M2 → M1 → 虚拟相机
```

即：

```text
C_virtual = R_M1(R_M2(C_real))
```

其中 \(R_M\) 表示关于镜面 \(M\) 的反射。

### 4.3 光轴与物体中心主光线的区别

这一点非常关键。

**虚拟相机光轴**是相机自身的几何属性：

```text
虚拟光心 → 完整传感器平面中点
```

而**物体中心主光线**是：

```text
真实液滴中心 → 虚拟光心 → 虚拟传感器上的成像点
```

二者通常不重合。

半幅 ROI 不是因为光轴穿过 ROI 中心，而是因为液滴通过反射路径在完整传感器上形成离轴图像。每个虚拟相机仍然保留完整传感器，只是有效 ROI 只占其中一半。

---

## 5. 当前系统参数汇总

以下参数从当前模拟程序中提取。

### 5.1 真实物体 / 液滴

| 参数 | 数值 |
|---|---:|
| 物体中心 | \((25, 0)\) mm |
| 模拟物体尺寸 | \(3.0\) mm |
| 液滴建议半径 | \(1.5\) mm |
| 水折射率 | \(n \approx 1.33\) |
| BOS 背景板间距初值 | \(g = 0.5\) mm |

### 5.2 真实相机等效模型

| 参数 | 数值 |
|---|---:|
| 真实光心 | \((200.391, 0)\) mm |
| 焦距 / 光心到传感器平面中点距离 | \(120.235\) mm |
| 传感器二维模拟长度 | \(8.445\) mm |
| 实际传感器尺寸 | \(8.445 \times 7.065\) mm |
| 像元尺寸 | \(3.45\) μm |
| 当前倍率 | \(0.6\times\) |

### 5.3 反射镜参数

| 反射镜 | 中心坐标 / mm | 长度 / mm | 角度 / ° |
|---|---:|---:|---:|
| M1 Down | \((65, -15.6)\) | 8 | 50.5 |
| M2 Down | \((57, -3.8)\) | 6 | 62 |
| M1 Up | \((65, 15.6)\) | 8 | -50.5 |
| M2 Up | \((57, 3.8)\) | 6 | -62 |

### 5.4 两个虚拟相机参数

| 参数 | Down 路径虚拟相机 | Up 路径虚拟相机 |
|---|---:|---:|
| 虚拟光心 \(C\) | \((211.5866, -73.7309)\) | \((211.5866, 73.7309)\) |
| 虚拟传感器中点 \(S_{\text{mid}}\) | \((322.2635, -120.7104)\) | \((322.2635, 120.7104)\) |
| 虚拟传感器上端 \(S_{\text{top}}\) | \((323.9133, -116.8236)\) | \((320.6136, 124.5973)\) |
| 虚拟传感器下端 \(S_{\text{bottom}}\) | \((320.6136, -124.5973)\) | \((323.9133, 116.8236)\) |
| 物体中心主光线交点 | \((323.4430, -117.9317)\) | \((323.4430, 117.9317)\) |
| 交点所在半区 | 上半 ROI | 下半 ROI |
| 虚拟光心到物体中心距离 | \(200.6260\) mm | \(200.6260\) mm |
| 虚拟光心到传感器中点距离 | \(120.2350\) mm | \(120.2350\) mm |

### 5.5 ROI 对应关系

| 真实传感器区域 | 对应虚像 | 对应反射路径 | 对应虚拟相机 |
|---|---|---|---|
| 上半区域 | 下方虚像 | Down 路径 | \(C_{\text{down}}\) |
| 下半区域 | 上方虚像 | Up 路径 | \(C_{\text{up}}\) |

---

## 6. 下一步任务

接下来应进入**光线追迹程序开发**。建议先做最小闭环：

```text
上方虚拟相机
    ↓
二维圆形液滴截面
    ↓
ROI 采样点逐条光线追迹
    ↓
求 Q0 和 Q
    ↓
输出 ΔQ
```

之后逐步扩展到：

```text
上方虚拟相机三维球形追迹
双虚拟相机三维球形追迹
椭球形液滴
生成模拟散斑图
模拟 DIC
真实实验 BOS/DIC
双视角轮廓 + BOS 优化
```

---

## 7. 当前最重要的结论

1. **虚拟相机法可行**，但不能把光轴和物体中心主光线混为一谈。
2. 每个虚拟相机应保留完整传感器平面，ROI 只是有效成像区域。
3. 光线追迹应从有效 ROI 像素出发，经虚拟光心反向延长到物方空间。
4. 在进入液滴前，光线必须是直线；只有在液滴界面处才发生折射。
5. 当前 BOS 建模第一步是求每个像素的 \(Q_0\) 和 \(Q\)。
